<div class="row">
  <h2><%= @post.title %></h2>
</div>

<div class="row">
  <div class="columns medium-6">
    <p><%= @post.body %></p>
  </div>
  <div class="columns medium-6">
    <h3>Comments</h3>
    <% @comments.each do |comment| %>
      <%= react_component('CommentSection') %>
    <% end %>
</div>
</div>
